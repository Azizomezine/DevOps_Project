apiVersion: v1
kind: Pod
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: app-devops
  name: app-devops
spec:
  containers:
    - env:
        - name: SPRING_APPLICATION_JSON
          value: '{ "spring.datasource.url": "jdbc:mysql://192.168.50.4:3306/mydb?createDatabaseIfNotExist=true", "spri>      image: azizomezine/devops:latest
      name: app-devops
      ports:
        - containerPort: 8082
          protocol: TCP
      stdin: true
      tty: true
  restartPolicy: OnFailure
